name: batch_ocr
description: 批量文字识别 - PaddleOCR 离线引擎
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_text_recognition: ^0.14.0
  file_picker: ^8.0.0
  path_provider: ^2.1.0
  permission_handler: ^11.0.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
